# Fantasy Fools Football HQ 2026 — Post-Draft Update

This package matches the existing Next.js/Vercel repository structure.

## Update GitHub
1. Keep your repository structure.
2. Replace the `app` folder with the `app` folder in this package.
3. Replace the `lib` folder with the `lib` folder in this package.
4. Replace `package.json`, `package-lock.json`, `vercel.json`, and `README.md` with these versions.
5. Leave `public/images` in place. This package includes the folder so GitHub/Vercel structure stays familiar.
6. Leave `supabase-schema.sql` in your repository unchanged. This update does not require a database schema change.
7. Commit changes. Vercel should redeploy automatically.

## Environment variables
Keep the Supabase environment variables already configured in Vercel. This build does not require changing them.
