import {NextRequest,NextResponse} from 'next/server';
export async function POST(req:NextRequest){const s=process.env.CRON_SECRET;if(s&&req.headers.get('x-cron-secret')!==s)return NextResponse.json({error:'Unauthorized'},{status:401});const base=process.env.NEXT_PUBLIC_SITE_URL||req.nextUrl.origin;let news:any={skipped:true};try{const r=await fetch(`${base}/api/sync/news`,{method:'POST',headers:{'x-cron-secret':s||''}});news=await r.json()}catch{}return NextResponse.json({ok:true,news,note:'Yahoo Fantasy data is intentionally not cron-cached; it is fetched live when HQ is opened.'});}
